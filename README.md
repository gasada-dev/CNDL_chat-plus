# CNDL_chat+

> Клиентский чат-менеджер и настраиваемый автоответчик для Minecraft 26.2 на Fabric.

![Version](https://img.shields.io/badge/version-0.3-B76EF5)
![Minecraft](https://img.shields.io/badge/Minecraft-26.2-62B47A)
![Fabric](https://img.shields.io/badge/loader-Fabric-DBD0B4)
![Environment](https://img.shields.io/badge/environment-client-6E8CF5)

CNDL_chat+ объединяет правила автоответа, маршрутизацию по каналам, фильтрацию
чата, периодические сообщения и быстрые действия с друзьями в одном GUI.
Экран мода открывается клавишей **F8**. Привязку можно изменить в стандартных
настройках управления Minecraft.

## Скачать

Готовая сборка хранится прямо в репозитории:

**[Скачать CNDL_chat+ 0.3](CNDL_chat+-0.3.jar)**

На странице файла в GitHub нажмите кнопку **Download raw file**.

## Возможности

### Правила автоответа

- любое количество правил «входящая фраза → ответ»;
- маски `*`: `привет*` подходит для фраз `привет`, `привет всем` и других
  продолжений;
- режимы `Авто`, `Локальный`, `Глобальный`, `Клановый` и `Личный`;
- первое подходящее правило имеет приоритет;
- собственные сообщения игрока не запускают автоответчик;
- защита от повторов и циклических ответов.

### Каналы чата

| Канал | Ответ по умолчанию |
|---|---|
| Локальный | Обычное сообщение |
| Глобальный | `!сообщение` |
| Клановый | `/. сообщение` |
| Личный | `/r сообщение` |

Префиксы и маркеры входящих сообщений можно менять под конкретный сервер.

### Чёрный список

- серверный мут игрока через `/ignoreplayer ник`;
- локальный мут Discord-пользователя с сохранением записи `ник (discord)`;
- распознавание форматов `[Discord] ник » сообщение` и `(Discord) ник ...`;
- общий переключатель отображения Discord-чата;
- чёрный список слов и фраз без учёта регистра;
- маски в словах: `реклама*`, `*казино*` и другие варианты;
- скрытые сообщения не передаются автоответчику.

### Друзья

- сохранённый список друзей с подсказками ников игроков на сервере;
- зелёная пометка `онлайн` и автоматическое обновление статуса;
- личное сообщение через `/w ник сообщение`;
- перевод денег через `/pay ник сумма`;
- запрос телепорта через `/call ник`.

### Периодические сообщения

Мод поддерживает до трёх независимых рассылок. Для каждой указываются текст,
интервал в минутах и состояние `Включено/Выключено`. Экран настройки открывается
скрытой областью слева от кнопки «Сохранить» во вкладке правил.

### Интерфейс

- четыре раздела: правила автоответа, каналы, чёрный список и друзья;
- подсказки при наведении на видимые кнопки;
- автодополнение ников клавишей `Tab`;
- автоматическое сохранение конфигурации;
- подпись `create by CNDL` с фирменным градиентом.

## Установка

1. Установите Minecraft 26.2 и Fabric Loader 0.19.3 или новее.
2. Установите Fabric API версии `0.156.0+26.2` или новее для Minecraft 26.2.
3. Поместите `CNDL_chat+-0.3.jar` в папку `.minecraft/mods`.
4. Запустите игру и нажмите `F8`.

Мод работает только на клиенте. Устанавливать его на сервер не требуется.

## Конфигурация

Настройки сохраняются в:

```text
.minecraft/config/gasada-chat-responder.json
```

Внутренний ID и имя файла конфигурации сохранены для совместимости с ранними
версиями мода.

## Сборка из исходников

Потребуется **JDK 25**. Репозиторий содержит Gradle Wrapper, поэтому отдельная
установка Gradle не нужна.

```bash
./gradlew build
```

Готовый JAR появится в `build/libs/`.

## Лицензия

Проект распространяется по лицензии [CC0-1.0](LICENSE).

**Автор: CNDL**
